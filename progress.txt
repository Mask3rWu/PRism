## Codebase Patterns
- Backend uses FastAPI with `lifespan` pattern for DB table creation on startup
- Config uses pydantic-settings `alias` (not `validation_alias`) for env var name mapping — use `populate_by_name=True` to allow both names
- Pass `connect_args={"check_same_thread": False}` for SQLite with FastAPI's async/thread-pool model
- Run backend from project root: `python -m uvicorn backend.main:app` (env_file path is relative to CWD)
- SQLAlchemy 2.0 ORM models: use `Mapped[]` + `mapped_column()`, `relationship()`, `back_populates` (not backref)
- Run mypy with `--explicit-package-bases` when checking code under `backend/` as a package
- Do not install `sqlalchemy2-stubs` — conflicts with SQLAlchemy 2.0's built-in typing
- Import all ORM model modules in `main.py` before `lifespan` so `Base.metadata` sees all tables at `create_all` time

---

## 2026/05/29 19:00 - US-002
- Created database models: Project, Review, AgentTiming in backend/models.py
- Updated backend/main.py to import models so tables are created on startup
- Recovered US-001 .py source files (config.py, database.py, main.py) which were never committed
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - Use `--explicit-package-bases` with mypy when the backend package is within the mypy check path
  - sqlalchemy2-stubs conflicts with SQLAlchemy 2.0 built-in typing — don't install it
  - Always import models before lifespan so Base.metadata knows about all table definitions
  - The scripts/ directory is gitignored; PRD and Ralph progress there are local-only
  - GitHub PR list API: GET /repos/{owner}/{repo}/pulls?state=open, response includes `number`, `title`, `user.login`, `created_at`, `head.ref`, `base.ref`
  - PR review status enrichment pattern: fetch PRs from GitHub, extract numbers, batch-query local Review table with `.in_()`, build a dict map, merge into response
  - When adding an API route under an existing path prefix, extend the same router file rather than creating a new one

---


## 2026/05/29 19:45 - US-004
- Created Project CRUD API endpoints: POST/GET/PUT/DELETE /api/projects
- Created backend/schemas/project.py with ProjectCreate, ProjectUpdate, ProjectResponse schemas
- Created backend/services/github/client.py with validate_pat() and get_repo() async helpers
- Created backend/api/projects.py with FastAPI router and SQLAlchemy session DI
- Registered projects router in backend/main.py
- Files changed: backend/main.py, backend/api/__init__.py, backend/api/projects.py, backend/schemas/__init__.py, backend/schemas/project.py, backend/services/__init__.py, backend/services/github/__init__.py, backend/services/github/client.py
- PR: #2 (branch: feat/US-004-project-crud-endpoints)
- **Learnings for future iterations:**
  - FastAPI project structure: schemas/ for Pydantic models, api/ for route handlers, services/ for external API calls
  - ProjectResponse uses ConfigDict(from_attributes=True) to serialize SQLAlchemy models directly
  - GitHub PAT validation: GET /repos/{owner}/{repo} with Bearer token; 401/403 = bad PAT, 404 = not found
  - DELETE endpoint returns 204 with no body — FastAPI handles this when the function returns None
  - All GitHub API calls use httpx.AsyncClient for async HTTP

---

## 2026/05/29 19:30 - US-003
- Created backend/core/security.py with encrypt_token() and decrypt_token() using Fernet
- Created backend/requirements.txt with all project dependencies (was missing, needed for US-001)
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - cryptography library's Fernet.generate_key() is the standard way to create keys
  - Fernet keys are url-safe base64-encoded 32-byte values
  - FERNET_KEY is already configured in Settings (config.py) and .env file
---


## 2026/05/29 18:15 - US-001
- Backend project scaffolding: FastAPI app, SQLite + SQLAlchemy setup, pydantic-settings config
- Files changed: backend/main.py, backend/core/__init__.py, backend/core/config.py, backend/core/database.py, backend/requirements.txt, .gitignore
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - pydantic-settings `validation_alias` does not work for env_file loading — use `alias` on Field instead
  - The .env file path in SettingsConfigDict is relative to CWD when running the app
  - Existing .env uses `DEFAULT_LLM_` prefix for LLM config vars
  - Added `__pycache__/` and `*.db` to .gitignore
---

## 2026/05/29 20:00 - US-005
- Created GET /api/projects/{id}/pulls endpoint: fetch open PRs from GitHub, merge with local review status
- Added fetch_pulls() to backend/services/github/client.py
- Created backend/schemas/pull_request.py with PullRequestItem schema
- Extended backend/api/projects.py router with list_pull_requests endpoint
- PR: #3 (branch: feat/US-005-pr-list-endpoint)
- **Learnings for future iterations:**
  - GitHub PR list API returns `number` (not `pr_number`), `user.login` (nested), `head.ref`/`base.ref` for branches
  - Batch query local Review table with `.in_(pr_numbers)` to enrich GitHub PRs with review status
  - For endpoints under an existing path prefix, extend the same router file rather than creating a new module
  - `httpx.Client.get(params=...)` needs explicit type annotation `dict[str, str | int]` when mixing str and int values
---

