## Codebase Patterns
- Backend uses FastAPI with `lifespan` pattern for DB table creation on startup
- Config uses pydantic-settings `alias` (not `validation_alias`) for env var name mapping — use `populate_by_name=True` to allow both names
- Pass `connect_args={"check_same_thread": False}` for SQLite with FastAPI's async/thread-pool model
- Run backend from project root: `python -m uvicorn backend.main:app` (env_file path is relative to CWD)
- SQLAlchemy 2.0 ORM models: use `Mapped[]` + `mapped_column()`, `relationship()`, `back_populates` (not backref)
- Run mypy with `--explicit-package-bases` when checking code under `backend/` as a package
- Do not install `sqlalchemy2-stubs` — conflicts with SQLAlchemy 2.0's built-in typing
- Import all ORM model modules in `main.py` before `lifespan` so `Base.metadata` sees all tables at `create_all` time
- New API routers go in `backend/api/<feature>.py`, register in `main.py` with `app.include_router()`, use `prefix="/api/<resource>"` and `tags=["<resource>"]`
- Use `joinedload()` for eager-loading SQLAlchemy relationships when the response schema needs them — avoids lazy-load DetachedInstanceError
- LangGraph TypedDict state: use `NotRequired` for optional fields and annotate at construction site to avoid `dict[str, object]` inference
- LLM calls via httpx: async POST to `{endpoint}/chat/completions` with Bearer token, parse `choices[0].message.content`
- Prompt templates in .md files: load at runtime with `open()`, substitute placeholders with `str.replace()`
- Agent nodes manage their own DB sessions via `SessionLocal()` — not via FastAPI's `Depends(get_db)`
- Graph nodes return `dict` (partial state updates) — return `{"summary_result": result}` not the full state
- `CompiledStateGraph` is the return type of `.compile()`, not `StateGraph`

---

## 2026/05/29 19:00 - US-002
- Created database models: Project, Review, AgentTiming in backend/models.py
- Updated backend/main.py to import models so tables are created on startup
- Recovered US-001 .py source files (config.py, database.py, main.py) which were never committed
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - Use `--explicit-package-bases` with mypy when the backend package is within the mypy check path
  - sqlalchemy2-stubs conflicts with SQLAlchemy 2.0 built-in typing — don't install it
  - Always import models before lifespan so Base.metadata knows about all table definitions
  - The scripts/ directory is gitignored; PRD and Ralph progress there are local-only
  - GitHub PR list API: GET /repos/{owner}/{repo}/pulls?state=open, response includes `number`, `title`, `user.login`, `created_at`, `head.ref`, `base.ref`
  - PR review status enrichment pattern: fetch PRs from GitHub, extract numbers, batch-query local Review table with `.in_()`, build a dict map, merge into response
  - When adding an API route under an existing path prefix, extend the same router file rather than creating a new one

---


## 2026/05/29 19:45 - US-004
- Created Project CRUD API endpoints: POST/GET/PUT/DELETE /api/projects
- Created backend/schemas/project.py with ProjectCreate, ProjectUpdate, ProjectResponse schemas
- Created backend/services/github/client.py with validate_pat() and get_repo() async helpers
- Created backend/api/projects.py with FastAPI router and SQLAlchemy session DI
- Registered projects router in backend/main.py
- Files changed: backend/main.py, backend/api/__init__.py, backend/api/projects.py, backend/schemas/__init__.py, backend/schemas/project.py, backend/services/__init__.py, backend/services/github/__init__.py, backend/services/github/client.py
- PR: #2 (branch: feat/US-004-project-crud-endpoints)
- **Learnings for future iterations:**
  - FastAPI project structure: schemas/ for Pydantic models, api/ for route handlers, services/ for external API calls
  - ProjectResponse uses ConfigDict(from_attributes=True) to serialize SQLAlchemy models directly
  - GitHub PAT validation: GET /repos/{owner}/{repo} with Bearer token; 401/403 = bad PAT, 404 = not found
  - DELETE endpoint returns 204 with no body — FastAPI handles this when the function returns None
  - All GitHub API calls use httpx.AsyncClient for async HTTP

---

## 2026/05/29 19:30 - US-003
- Created backend/core/security.py with encrypt_token() and decrypt_token() using Fernet
- Created backend/requirements.txt with all project dependencies (was missing, needed for US-001)
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - cryptography library's Fernet.generate_key() is the standard way to create keys
  - Fernet keys are url-safe base64-encoded 32-byte values
  - FERNET_KEY is already configured in Settings (config.py) and .env file
---


## 2026/05/29 18:15 - US-001
- Backend project scaffolding: FastAPI app, SQLite + SQLAlchemy setup, pydantic-settings config
- Files changed: backend/main.py, backend/core/__init__.py, backend/core/config.py, backend/core/database.py, backend/requirements.txt, .gitignore
- PR: N/A (no remote configured)
- **Learnings for future iterations:**
  - pydantic-settings `validation_alias` does not work for env_file loading — use `alias` on Field instead
  - The .env file path in SettingsConfigDict is relative to CWD when running the app
  - Existing .env uses `DEFAULT_LLM_` prefix for LLM config vars
  - Added `__pycache__/` and `*.db` to .gitignore
---

## 2026/05/29 20:00 - US-005
- Created GET /api/projects/{id}/pulls endpoint: fetch open PRs from GitHub, merge with local review status
- Added fetch_pulls() to backend/services/github/client.py
- Created backend/schemas/pull_request.py with PullRequestItem schema
- Extended backend/api/projects.py router with list_pull_requests endpoint
- PR: #3 (branch: feat/US-005-pr-list-endpoint)
- **Learnings for future iterations:**
  - GitHub PR list API returns `number` (not `pr_number`), `user.login` (nested), `head.ref`/`base.ref` for branches
  - Batch query local Review table with `.in_(pr_numbers)` to enrich GitHub PRs with review status
  - For endpoints under an existing path prefix, extend the same router file rather than creating a new module
  - `httpx.Client.get(params=...)` needs explicit type annotation `dict[str, str | int]` when mixing str and int values
---

## 2026/05/29 21:00 - US-006
- Implemented POST /api/projects/{id}/pulls/{pr_number}/review trigger endpoint
- Added `diff_content` column to Review model for storing PR diff (needed by later agent graph stories)
- Added `fetch_pr_detail()` and `fetch_pr_diff()` to GitHub client for single-PR operations
- Created `backend/services/review/service.py` with asyncio.Semaphore(3) concurrency limiting
- Background task: fetches PR diff, updates status (queued→running→succeeded/failed) and stage (fetching_diff→diff_fetched)
- Duplicate trigger detection: rejects if a review is already queued/running for the same PR (409)
- Files changed: backend/models.py, backend/api/projects.py, backend/services/github/client.py, backend/schemas/review.py, backend/services/review/__init__.py, backend/services/review/service.py
- PR: #4 (branch: feat/US-006-trigger-review-endpoint)
- **Learnings for future iterations:**
  - FastAPI BackgroundTasks support async functions natively — just pass the coroutine function reference
  - Use asyncio.Semaphore for in-process concurrency limiting (no need for external task queue)
  - Background tasks need their own DB session (cannot use Depends) — use SessionLocal() directly
  - The `from_attributes=True` ConfigDict on Pydantic models serializes SQLAlchemy Enum fields as their string values
  - GitHub PR diff endpoint: same URL as PR detail, but with `Accept: application/vnd.github.v3.diff` header
  - Future agent graph stories (US-008+) should extend `_run_review_background` rather than replacing it
---

## 2026/05/29 - US-007
- Implemented Review status and detail query endpoints:
  - GET /api/reviews/{id}/status — returns status, stage, error_message, agent timings
  - GET /api/reviews/{id}/status — returns full review result (summary, risk, issues, tests, comment) + agent_timings
- Created backend/api/reviews.py with APIRouter(prefix="/api/reviews", tags=["reviews"])
- Added schemas: AgentTimingItem, ReviewStatusResponse, ReviewDetailResponse (extends ReviewResponse)
- Registered reviews router in backend/main.py
- Files changed: backend/api/reviews.py (new), backend/schemas/review.py, backend/main.py
- PR: #5 (branch: feat/US-007-review-status-endpoints)
- **Learnings for future iterations:**
  - SQLite DateTime columns require Python datetime objects, not ISO strings
  - FastAPI TestClient needs context manager usage (`with TestClient(app) as client:`) to trigger lifespan
  - Deleting SQLite DB file and restarting re-creates tables via `Base.metadata.create_all` in lifespan
  - `ReviewDetailResponse(ReviewResponse)` inherits all fields from ReviewResponse and adds `agent_timings`
  - Endpoint order in FastAPI matters — `/{review_id}/status` must be defined before `/{review_id}` to avoid path capture
---

## 2026/05/29 - US-008
- Implemented LangGraph Summary Agent node: LLM-powered PR diff summarization
- Created backend/core/llm_client.py: async LLM client using httpx for OpenAI-compatible API
- Created backend/agents/ package: states.py (ReviewState TypedDict), review_graph.py (START→summary→END DAG), nodes/summary.py (Summary Agent node)
- Created backend/agents/prompts/summary.md: prompt template with {project_description} and {pr_diff} placeholders
- Extended backend/services/review/service.py: graph invocation after diff fetch, succeeded/failed status handling
- PR: #6 (branch: feat/US-008-summary-agent-node)
- **Learnings for future iterations:**
  - LangGraph nodes manage their own DB sessions via SessionLocal() — no FastAPI Depends in graph context
  - Use NotRequired for optional TypedDict fields in state schema
  - Graph node functions return partial state dict, not full state
  - `llm_call_json()` strips markdown code fences from LLM responses before JSON parsing
  - Prompt templates use str.replace() for placeholder substitution (keeps it simple)
  - Future nodes (US-009~US-012) should follow the same pattern: own DB session, record AgentTiming, update review stage
  - The graph is compiled once at module load time — shared across all requests
---

## 2026/05/29 - US-009
- Implemented LangGraph Risk Analysis Agent node: LLM-powered risk identification from PR diff
- Created backend/agents/prompts/risk_analysis.md: prompt template for risk categories (auth, DB, concurrency, security, error handling, performance)
- Created backend/agents/nodes/risk_analysis.py: follows same pattern as summary_node (own SessionLocal(), AgentTiming recording, review stage update)
- Extended ReviewState with risk_result field; wired risk_analysis sequentially after summary in review_graph.py
- PR: #7 (branch: feat/US-009-risk-analysis-node)
- **Learnings for future iterations:**
  - New agent nodes follow the exact same pattern: SessionLocal() for DB, start/end time AgentTiming, review.stage before and after LLM call
  - risk_analysis node passes summary_result as JSON string into the prompt for additional context
  - The review_graph.py mypy return-type warning is pre-existing (LangGraph generic inference limitation) — safe to ignore
---

## 2026/05/29 - US-010
- Implemented Issue Detection Agent node: LLM-powered bug detection from PR diff
- Created backend/agents/prompts/issue_detection.md: prompt template covering 11 issue categories (null refs, unhandled exceptions, SQL injection, race conditions, missing await, resource leaks, error propagation, type mismatches, off-by-one, logic errors, hardcoded secrets)
- Created backend/agents/nodes/issue_detection.py: follows same pattern as summary/risk_analysis nodes (own SessionLocal(), AgentTiming recording, stage update)
- Extended ReviewState with issue_result field; wired issue_detection sequentially after risk_analysis in review_graph.py
- PR: #8 (branch: feat/US-010-issue-detection-node)
- **Learnings for future iterations:**
  - Agent nodes are fully templated — copy risk_analysis.py, rename function, change stage strings and prompt path
  - Adding a new agent node requires touching exactly 4 files: prompt .md, node .py, states.py, review_graph.py
---